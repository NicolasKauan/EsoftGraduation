#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct pessoa {
    char nome[50], cpf[15], rg[15], cel[16], fixo[16];
    char endereco[50], bairro[30], cidade[30], uf[3];
    int idade;
};
int main (){
    struct pessoa cl[50];
    
    











    return 0;
}