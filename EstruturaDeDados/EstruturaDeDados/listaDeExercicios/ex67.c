#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>
/* Desenvolva um programa em linguagem C que realize o cadastro de pessoas, com as
seguintes funcionalidades: inclusão, alteração, exclusão e consulta de registros.
As informações de cada pessoa devem ser armazenadas em uma estrutura (struct) contendo os
seguintes campos:
• Nome
• Endereço
• Cidade
• Sexo
• CPF
• RG
• E-mail
Todos os campos devem ser alocados dinamicamente (uso de malloc, realloc, etc.) de acordo com
a necessidade de armazenamento de strings e structs.
As estruturas deverão ser organizadas em um vetor dinâmico de ponteiros, permitindo a
manipulação flexível da quantidade de registros conforme o programa é executado.
Implemente um menu interativo que permita ao usuário escolher entre as operações de cadastro,
edição, exclusão e consulta de registros. Garanta a liberação adequada da memória ao final do
programa.*/

int main(){
    







    return 0;
}

typedef struct{

}cadastro;

