#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main(){
    // Ponteiro
    //int *p;
    // p = malloc(3*sizeof(int));
    
    // Ponteiro para ponteiro
    // int **p;
    // p = malloc(3*sizeof(int*));
    // for (x=0; x<3; x++){
        // p[x]=malloc(5*sizeof(int));
    // }

    // Ponteiro de ponteiro de ponteiro
    // char ***p, nome[50];
    // p = malloc(3*sizeof(char**));
    // for(x=0; x<3; x++){
    //      p[x]=malloc(5*sizeof(char*));
    //          for(int y = 0; y<5; y++){
    //              gets(nome);
    //              n = strlen(nome)
    //              p[x][y]= malloc(n+1);
    //              strcpy(p[x][y],nome);
    //          }
    // }









    return 0;
}
